# openspec/config.yaml
# Auto-generated by sdd-init on 2026-06-11
schema: spec-driven

context: |
  Project: AgentTrail (@aivoralabs/agenttrail)
  Stack: TypeScript 5.5+, Node 22, pnpm 9.0.0, Turbo repo
  Architecture: In-process audit trail SDK (hash chain + Ed25519 receipts)
  Monorepo: packages/core, packages/openai, packages/vercel-ai, packages/cli
  Apps: apps/landing (Astro), apps/docs (VitePress), apps/example
  Build: tsup (ESM-only, dual CJS/ESM for core)
  Testing: Vitest 2.x, @fast-check/vitest (property-based)
  Lint/Format: Biome 1.9.0
  Type check: tsc --noEmit via tsconfig.typecheck.json
  CI: GitHub Actions (typecheck → build → test → coverage → lint)
  Key deps: @noble/ed25519, uuid v11, zod v3 (core); ai v4 (vercel-ai); openai v4 (openai)
  Crypto: Web Crypto API (SHA-256), zero-dep hash chain

testing:
  framework: vitest
  version: ^2.0.0
  runner_command: vitest run
  coverage_command: vitest run --coverage
  coverage_tool: "@vitest/coverage-v8"
  layers:
    unit: true
    integration: false
    e2e: true
  e2e_script: e2e-test.sh
  e2e_requires: GROQ_API_KEY
  strict_tdd: true
  property_based: true
  property_framework: "@fast-check/vitest"
  test_location: __tests__/ (per package, alongside modules)

rules:
  proposal:
    - Include rollback plan for risky changes
    - Scope must clearly separate In vs Out
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
    - Always include test tasks alongside implementation
  apply:
    - Follow existing code patterns (ESM, strict TS, Biome style)
    - tdd: true
    - test_command: pnpm --filter {package} test
  verify:
    - test_command: pnpm test
    - build_command: pnpm build
    - typecheck_command: pnpm typecheck
    - lint_command: pnpm lint
    - coverage_threshold: 80
  archive:
    - Warn before merging destructive deltas
    - Move change folder to archive/ with YYYY-MM-DD prefix
